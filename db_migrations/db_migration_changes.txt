- create user table with script 
- add role column to user table
- add invite token table 
- rename rack# column to stop postgres error

-- Add QueuePosition column to WorkOrder table (tblcustworkorderdetail) for manual queue reordering
-- Run this SQL script to add the new column

ALTER TABLE tblcustworkorderdetail
ADD COLUMN QueuePosition INTEGER DEFAULT NULL;

-- Create index for better performance when ordering by queue position
CREATE INDEX idx_workorder_queue_position ON tblcustworkorderdetail(QueuePosition);

-- Optional: Create a composite index for cleaning queue queries
CREATE INDEX idx_workorder_cleaning_queue ON WorkOrder(Clean, DateCompleted, QueuePosition, FirmRush, RushOrder, DateRequired, DateIn);